# 🎤 RVC Voice Conversion API Service

Отдельный микросервис для конвертации голоса через RVC модель. Оптимизирован для работы на хостинге с ограниченными ресурсами (5-6 ГБ RAM).

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Настройка](#-настройка)
- [Запуск](#-запуск)
- [API Endpoints](#-api-endpoints)
- [Интеграция с ботом](#-интеграция-с-ботом)
- [Оптимизация для хостинга](#-оптимизация-для-хостинга)
- [Troubleshooting](#-troubleshooting)

---

## 🚀 Быстрый старт

```bash
# 1. Перейдите в папку сервиса
cd rvc_service

# 2. Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Запустите сервис
uvicorn main:app --host 0.0.0.0 --port 8001
```

Сервис будет доступен по адресу: `http://localhost:8001`

---

## 📦 Установка

### Требования

- Python 3.10+
- 5-6 ГБ свободной RAM (для модели)
- Модель RVC (файлы `.pth` и `.index`)

### Шаги установки

1. **Клонируйте репозиторий** (если еще не сделано)

2. **Установите зависимости:**

```bash
pip install -r requirements.txt
```

**Примечание:** Установка может занять 10-15 минут из-за больших зависимостей (torch, fairseq и т.д.)

3. **Проверьте наличие модели:**

Убедитесь, что файлы модели находятся в папке `Miaea/`:
- `Miaea/model.pth` - основная модель
- `Miaea/model.index` - индексный файл (опционально)

---

## ⚙️ Настройка

Настройка выполняется через переменные окружения. Создайте файл `.env` в папке `rvc_service/`:

```env
# Пути к модели
RVC_MODEL_PATH=Miaea/model.pth
RVC_INDEX_PATH=Miaea/model.index

# Устройство обработки (cpu или cuda)
RVC_DEVICE=cpu

# Параметры RVC
RVC_F0_UP_KEY=0
RVC_INDEX_RATE=0.75
RVC_PROTECT=0.33

# Порт сервиса
RVC_PORT=8001
```

### Параметры RVC

- **`RVC_F0_UP_KEY`**: Сдвиг высоты тона (в полутонах). Обычно `0` или `±12`.
- **`RVC_INDEX_RATE`**: Интенсивность использования индексного файла (0.0 - 1.0). Рекомендуется `0.75`.
- **`RVC_PROTECT`**: Защита от артефактов (0.0 - 1.0). Рекомендуется `0.33`.

---

## 🏃 Запуск

### Локальный запуск

```bash
uvicorn main:app --host 0.0.0.0 --port 8001
```

### Запуск на хостинге (systemd)

Создайте файл `/etc/systemd/system/rvc-service.service`:

```ini
[Unit]
Description=RVC Voice Conversion API Service
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/rvc_service
Environment="RVC_MODEL_PATH=Miaea/model.pth"
Environment="RVC_INDEX_PATH=Miaea/model.index"
Environment="RVC_DEVICE=cpu"
Environment="RVC_PORT=8001"
ExecStart=/path/to/rvc_service/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8001 --workers 1
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:

```bash
sudo systemctl enable rvc-service
sudo systemctl start rvc-service
sudo systemctl status rvc-service
```

### Запуск через screen/tmux

```bash
screen -S rvc-service
cd /path/to/rvc_service
source venv/bin/activate
uvicorn main:app --host 0.0.0.0 --port 8001
# Нажмите Ctrl+A, затем D для отсоединения
```

---

## 📡 API Endpoints

### `GET /health`

Проверка работоспособности сервиса.

**Response:**
```json
{
  "status": "ok",
  "model_loaded": true,
  "model_error": null,
  "device": "cpu",
  "model_path": "Miaea/model.pth",
  "index_path": "Miaea/model.index"
}
```

### `POST /convert`

Конвертирует голос через RVC модель.

**Request:**
- `Content-Type: multipart/form-data`
- `audio`: OGG файл (обязательно)

**Response:**
- `Content-Type: audio/ogg`
- Конвертированное аудио

**Headers:**
- `X-Processing-Time`: Время обработки в секундах
- `X-Original-Size`: Размер исходного файла (байты)
- `X-Converted-Size`: Размер конвертированного файла (байты)

**Пример запроса (curl):**
```bash
curl -X POST "http://localhost:8001/convert" \
  -F "audio=@voice.ogg" \
  -o converted.ogg
```

**Пример запроса (Python):**
```python
import aiohttp

async def convert_voice(audio_bytes: bytes) -> bytes:
    async with aiohttp.ClientSession() as session:
        form = aiohttp.FormData()
        form.add_field(
            "audio",
            audio_bytes,
            filename="voice.ogg",
            content_type="audio/ogg"
        )
        
        async with session.post(
            "http://localhost:8001/convert",
            data=form
        ) as response:
            if response.status == 200:
                return await response.read()
            else:
                raise Exception(f"Ошибка: {response.status}")
```

---

## 🔗 Интеграция с ботом

### 1. Добавьте URL RVC API в `.env` бота:

```env
API_RVC=http://localhost:8001/convert
```

Если RVC сервис на другом сервере:
```env
API_RVC=http://your-rvc-server.com:8001/convert
```

### 2. Бот автоматически использует RVC

Код бота уже готов! Метод `apply_rvc_remote` в `user_service.py` автоматически:
- Отправляет аудио в RVC API
- Обрабатывает ошибки (возвращает исходное аудио при ошибке)
- Логирует все операции

### 3. Проверка работы

```bash
# Проверьте что RVC сервис работает
curl http://localhost:8001/health

# Проверьте что бот видит RVC API
# В логах бота должно быть: "RVC успешно применён"
```

---

## ⚡ Оптимизация для хостинга

### Ресурсы

- **RAM**: ~2-3 ГБ (модель загружается один раз в память)
- **CPU**: 1-2 ядра достаточно для обработки
- **Время обработки**: 5-8 секунд на запрос (зависит от длины аудио)
- **Workers**: **1** (модель не потокобезопасна, не используйте несколько workers!)

### Рекомендации

1. **Используйте CPU** если нет GPU (установите `RVC_DEVICE=cpu`)
2. **Один worker** (`--workers 1`) - обязательно!
3. **Мониторинг**: Следите за использованием RAM через `htop` или `free -h`
4. **Автоперезапуск**: Настройте systemd для автоматического перезапуска при падении

### Ограничения

- **Одновременные запросы**: 1 (из-за одного worker)
- **Таймаут бота**: 30 секунд (достаточно для обработки)
- **Максимальный размер аудио**: Рекомендуется до 30 секунд

---

## 🔧 Troubleshooting

### Ошибка: "rvc-python не установлен"

```bash
pip install rvc-python
```

Если не помогает, установите зависимости вручную:
```bash
pip install torch torchaudio soundfile numpy scipy librosa faiss-cpu
```

### Ошибка: "fairseq не компилируется"

**Windows:**
1. Установите [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/)
2. Выберите "C++ build tools"
3. Перезапустите терминал
4. Попробуйте: `pip install fairseq --no-build-isolation`

**Linux:**
```bash
sudo apt-get update
sudo apt-get install build-essential
pip install fairseq --no-build-isolation
```

### Ошибка: "Модель не найдена"

Проверьте пути в переменных окружения:
```bash
# Убедитесь что файлы существуют
ls -la Miaea/model.pth
ls -la Miaea/model.index

# Проверьте переменные окружения
echo $RVC_MODEL_PATH
```

### Ошибка: "Out of memory"

1. Убедитесь что используете `RVC_DEVICE=cpu` (не cuda)
2. Закройте другие приложения
3. Проверьте доступную RAM: `free -h`
4. Уменьшите `RVC_INDEX_RATE` до `0.5`

### Сервис не отвечает

1. Проверьте логи: `journalctl -u rvc-service -f`
2. Проверьте что порт свободен: `netstat -tulpn | grep 8001`
3. Проверьте firewall: `sudo ufw allow 8001`

### Медленная обработка

- Это нормально! RVC обработка занимает 5-8 секунд
- Если очень медленно (>15 сек), проверьте нагрузку CPU
- Убедитесь что не запущено несколько workers

---

## 📊 Мониторинг

### Проверка статуса

```bash
# Health check
curl http://localhost:8001/health

# Проверка через systemd
sudo systemctl status rvc-service

# Просмотр логов
journalctl -u rvc-service -f
```

### Метрики производительности

В заголовках ответа `/convert` есть метрики:
- `X-Processing-Time`: Время обработки
- `X-Original-Size`: Размер исходного файла
- `X-Converted-Size`: Размер результата

---

## 📝 Лицензия

Этот сервис является частью проекта OpenMitAIbot.

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи сервиса
2. Проверьте логи бота
3. Убедитесь что все зависимости установлены
4. Проверьте что модель существует и доступна

---

**Успешной работы! 🎉**

